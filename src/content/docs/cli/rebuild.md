---
title: "pnpm rebuild"
headingIds: ["options","--recursive--r","--filter-package_selector"]
---

别名： `rb`

重新构建一个包。

## 选项

### --recursive, -r

此命令会在 monorepo 的每个包中运行 **pnpm rebuild** 命令。

### --filter <package_selector>

[Read more about filtering.](/docs/filtering)
